<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <% if current_user %>
      <a class="navbar-brand" href="/" > (｡･∀･)ﾉﾞ <%= current_user.nickname %> </a>
      <% else %>
      <a class="navbar-brand" href="/"> (｡･∀･)ﾉﾞ </a>
      <% end %>
    </div>
    <ul class="nav navbar-nav">
      <li><%= link_to 'Posts', posts_path, {:style=>'color: white;'}  %></li> 
      <li><%= link_to 'Categories', categories_path, {:style=>'color: white;'} %></li> 
    </ul>

    <ul class="nav navbar-nav navbar-right">
      <% if current_user %>
      <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#">Profile
        <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><%= link_to 'Profile', user_path(current_user.id) %></li>
          <li><%= link_to 'Gallery', galleries_path(current_user) %></li>
          <li><%= link_to 'Setting', edit_user_path(current_user) %></li>
          <li><%= link_to 'Sign out', logout_path%></li>
        </ul>
      </li>

      <% else %>
        <li><%= link_to 'Sign in', new_session_path, {:style=>'color: white;'} %></li>
        <li><%= link_to 'Sign up', new_user_path, {:style=>'color: white;'} %></li>
      <% end %>
    </ul>
  </div>
</nav>
<hr>

<%= render 'shared/messages' %>

